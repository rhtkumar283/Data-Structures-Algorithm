//*********DATA STRUCTURES********//

//============LinkedList==========//
//==SinglyLinkedList==//
//printList
//listLength
//Push --add node at the beginning (before the head);
//Append --add node at the end of the list;
//Insert --add node after a given node in the list;
//removetheHead
//removeAtEnd
//deleteNodeAt --given the key of a node delete that node;
//getPosition --To get the position of the given data in linked linked list
//reverseList --reverse the linked listLength
//removeNodeAtPosition --Given the position remove the respective node from the list

///==DoublyLinkedList==//
//Printing the entire DLL
//Printing the length of the list
//Adding node at the starting of the list
//Adding node at the end of the list
//Adding node after a given node
//Inserting a node before a given node
//Remove the node with the matching key.
//Get the position of the node based on the given key.
//Given the position remove the respective node from the list
//To clear the list.

//==CircularLinkedList==//
//printList
//listLength
//Push --add node at the beginning (before the head);
//Append --add node at the end of the list;
//Insert --add node after a given node in the list;
//removetheHead
//removeAtEnd
//deleteNodeAt --given the key of a node delete that node;
//getPosition --To get the position of the given data in linked linked list
//Given the position remove the respective node from the list
//To clear the list


//=============Stack==============//
//===FixedLengthStack===//
//To get the size of the stack;
//To check if the stack is empty;
//To insert an element in the stack;
//To get the top element of the stack;
//To remove an element from the stack;
//Printing the stack:

//==DynamicArrayStack==//
//To get the size of the stack;
//To check if the stack is empty;
//To insert an element in the stack;
//To expand the Darray if full; expand();
//To reduce the DArray if we can; reduce();
//To get the top element of the stack;
//To remove an element from the stack;
//Printing the stack;

//==LinkedListStack==//
//Check if the list is Empty;
//To get the size of the stack;
//Print the list;
//Add the given data to the stack list;
//Add the given data to the stack list;
//Remove the element from the stack (from the top or head of list stack);
//To take a peek at the top element in stack list;


//==============Queues==============//
//==FixedSizeArrayQueue==//
//to see if the queue is empty:
//to see if the queue is full:
//To print the size of the queue;
//to EnQueue i.e. to add an element in queue:
//to DeQueue i.e. to remove an element from the queue;
//To get the front of the queue;
//To get the rear of the queue;
//To print the queue;

//==DynamicArrayQueue==//
//to see if the queue is empty:
//to see if the queue is full:
//To print the size of the queue;
//to EnQueue i.e. to add an element in queue:
//expand();
//reduce();
//to DeQueue i.e. to remove an element from the queue;
//To get the front of the queue;
//To get the rear of the queue;
//To print the queue;

//==LinkedListQueue==//
//to get the length of the queue;
//to see if list is empty:
//to get the front element;
//to get the rear of the list;
//Enqueue;
//DeQueue;
//To print the queue:


//===================Trees=================//
